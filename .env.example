# SERVER ONLY. Copy to .env and add your key locally.
# Never embed these values in index.html or deploy .env to GitHub Pages.
MOONSHOT_API_KEY=
KIMI_MODEL=kimi-k3
KIMI_BASE_URL=https://api.moonshot.ai/v1

# Future server deployment: restrict requests to the published frontend.
# Origin checking is not authentication; add rate limits and abuse controls.
ALLOWED_ORIGIN=https://stanleycheng.github.io

# No AI backend is deployed by this initial static release.
